# Z 리디자인 이관 기록

- 원본: `Z:\ai_common_share\korea-medical-redesign\design-system.html`
- 보관본: `docs/legacy-redesign-design-system.html`
- 원본 SHA-256: `88B364E8D25CC3B24B7BA7CAC12D95A4A3FE4FDB09B0467FB97429066612CCA3`
- 상태: 원본과 동일한 참고용 보관본. 현재 UI 정본은 워크스페이스 WaveNet 가이드다.

## 보존한 설계 원칙

- 데이터가 주인공이며 숫자·차트·표를 우선한다.
- 위험·접근성·주의·정상 상태는 색과 함께 라벨을 제공한다.
- 섹션마다 제목 자체가 핵심 결론이 되도록 구성한다.
- 최소 4.5:1 대비, 키보드 포커스, 시맨틱 랜드마크를 유지한다.
- 숫자에는 tabular numerals를 적용한다.

## 원본 토큰과 컴포넌트 범위

- 4px 간격 체계, 11.5–44px 타이포 스케일, 시스템 산세리프·고정폭 숫자 폰트.
- 상태색: critical `#dc2626`, warn `#d97706`, access `#0d9488`, ok `#16a34a`, info `#2563eb`.
- KPI, 버튼, 배지, 세그먼트 컨트롤, 데이터 표, 맨 위로 이동 버튼을 정의했다.

## 통합 결정

- KPI·표·세그먼트·접근성 원칙과 `toTop` 동작은 현재 `index.html`에 통합했다.
- 원본의 다크 기본 변형, 다중 그라디언트, 10px 초과 반경과 그림자는 WaveNet 전역 계약과 충돌해 런타임 스타일로 이식하지 않았다.
- 원본 예시 수치(48개 병원·250개 지역)는 최신 정본(공식 47개·252개)으로 대체했다.
